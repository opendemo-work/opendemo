apiVersion: apps/v1
kind: Deployment
metadata:
  name: platform-engineering
  labels:
    app: platform-engineering
spec:
  replicas: 1
  selector:
    matchLabels:
      app: platform-engineering
  template:
    metadata:
      labels:
        app: platform-engineering
    spec:
      containers:
      - name: demo
        image: nginx:alpine
        ports:
        - containerPort: 80
