apiVersion: apps/v1
kind: Deployment
metadata:
  name: prometheus-integration
  labels:
    app: prometheus-integration
spec:
  replicas: 1
  selector:
    matchLabels:
      app: prometheus-integration
  template:
    metadata:
      labels:
        app: prometheus-integration
    spec:
      containers:
      - name: demo
        image: nginx:alpine
        ports:
        - containerPort: 80
