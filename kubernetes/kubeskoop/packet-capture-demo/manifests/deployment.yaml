apiVersion: apps/v1
kind: Deployment
metadata:
  name: packet-capture-demo
  labels:
    app: packet-capture-demo
spec:
  replicas: 1
  selector:
    matchLabels:
      app: packet-capture-demo
  template:
    metadata:
      labels:
        app: packet-capture-demo
    spec:
      containers:
      - name: demo
        image: nginx:alpine
        ports:
        - containerPort: 80
