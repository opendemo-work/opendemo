apiVersion: apps/v1
kind: Deployment
metadata:
  name: event-probes-configuration
  labels:
    app: event-probes-configuration
spec:
  replicas: 1
  selector:
    matchLabels:
      app: event-probes-configuration
  template:
    metadata:
      labels:
        app: event-probes-configuration
    spec:
      containers:
      - name: demo
        image: nginx:alpine
        ports:
        - containerPort: 80
