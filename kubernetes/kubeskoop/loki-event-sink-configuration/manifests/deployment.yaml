apiVersion: apps/v1
kind: Deployment
metadata:
  name: loki-event-sink-configuration
  labels:
    app: loki-event-sink-configuration
spec:
  replicas: 1
  selector:
    matchLabels:
      app: loki-event-sink-configuration
  template:
    metadata:
      labels:
        app: loki-event-sink-configuration
    spec:
      containers:
      - name: demo
        image: nginx:alpine
        ports:
        - containerPort: 80
