apiVersion: apps/v1
kind: Deployment
metadata:
  name: network-cni
  labels:
    app: network-cni
spec:
  replicas: 1
  selector:
    matchLabels:
      app: network-cni
  template:
    metadata:
      labels:
        app: network-cni
    spec:
      containers:
      - name: demo
        image: nginx:alpine
        ports:
        - containerPort: 80
