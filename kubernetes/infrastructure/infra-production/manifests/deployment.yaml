apiVersion: apps/v1
kind: Deployment
metadata:
  name: infra-production
  labels:
    app: infra-production
spec:
  replicas: 1
  selector:
    matchLabels:
      app: infra-production
  template:
    metadata:
      labels:
        app: infra-production
    spec:
      containers:
      - name: demo
        image: nginx:alpine
        ports:
        - containerPort: 80
