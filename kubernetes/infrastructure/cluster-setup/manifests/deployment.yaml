apiVersion: apps/v1
kind: Deployment
metadata:
  name: cluster-setup
  labels:
    app: cluster-setup
spec:
  replicas: 1
  selector:
    matchLabels:
      app: cluster-setup
  template:
    metadata:
      labels:
        app: cluster-setup
    spec:
      containers:
      - name: demo
        image: nginx:alpine
        ports:
        - containerPort: 80
