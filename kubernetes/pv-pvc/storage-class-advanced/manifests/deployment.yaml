apiVersion: apps/v1
kind: Deployment
metadata:
  name: storage-class-advanced
  labels:
    app: storage-class-advanced
spec:
  replicas: 1
  selector:
    matchLabels:
      app: storage-class-advanced
  template:
    metadata:
      labels:
        app: storage-class-advanced
    spec:
      containers:
      - name: demo
        image: nginx:alpine
        ports:
        - containerPort: 80
