apiVersion: apps/v1
kind: Deployment
metadata:
  name: pv-pvc
  labels:
    app: pv-pvc
spec:
  replicas: 1
  selector:
    matchLabels:
      app: pv-pvc
  template:
    metadata:
      labels:
        app: pv-pvc
    spec:
      containers:
      - name: demo
        image: nginx:alpine
        ports:
        - containerPort: 80
