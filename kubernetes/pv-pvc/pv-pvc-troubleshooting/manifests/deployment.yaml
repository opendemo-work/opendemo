apiVersion: apps/v1
kind: Deployment
metadata:
  name: pv-pvc-troubleshooting
  labels:
    app: pv-pvc-troubleshooting
spec:
  replicas: 1
  selector:
    matchLabels:
      app: pv-pvc-troubleshooting
  template:
    metadata:
      labels:
        app: pv-pvc-troubleshooting
    spec:
      containers:
      - name: demo
        image: nginx:alpine
        ports:
        - containerPort: 80
