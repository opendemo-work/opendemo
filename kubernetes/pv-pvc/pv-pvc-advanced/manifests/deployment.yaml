apiVersion: apps/v1
kind: Deployment
metadata:
  name: pv-pvc-advanced
  labels:
    app: pv-pvc-advanced
spec:
  replicas: 1
  selector:
    matchLabels:
      app: pv-pvc-advanced
  template:
    metadata:
      labels:
        app: pv-pvc-advanced
    spec:
      containers:
      - name: demo
        image: nginx:alpine
        ports:
        - containerPort: 80
