apiVersion: apps/v1
kind: Deployment
metadata:
  name: pv-pvc-production
  labels:
    app: pv-pvc-production
spec:
  replicas: 1
  selector:
    matchLabels:
      app: pv-pvc-production
  template:
    metadata:
      labels:
        app: pv-pvc-production
    spec:
      containers:
      - name: demo
        image: nginx:alpine
        ports:
        - containerPort: 80
