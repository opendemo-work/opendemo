apiVersion: apps/v1
kind: Deployment
metadata:
  name: pv-pvc-basics
  labels:
    app: pv-pvc-basics
spec:
  replicas: 1
  selector:
    matchLabels:
      app: pv-pvc-basics
  template:
    metadata:
      labels:
        app: pv-pvc-basics
    spec:
      containers:
      - name: demo
        image: nginx:alpine
        ports:
        - containerPort: 80
