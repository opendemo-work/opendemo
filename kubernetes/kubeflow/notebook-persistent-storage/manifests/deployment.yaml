apiVersion: apps/v1
kind: Deployment
metadata:
  name: notebook-persistent-storage
  labels:
    app: notebook-persistent-storage
spec:
  replicas: 1
  selector:
    matchLabels:
      app: notebook-persistent-storage
  template:
    metadata:
      labels:
        app: notebook-persistent-storage
    spec:
      containers:
      - name: demo
        image: nginx:alpine
        ports:
        - containerPort: 80
