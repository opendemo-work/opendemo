apiVersion: apps/v1
kind: Deployment
metadata:
  name: dashboard-rbac-configuration
  labels:
    app: dashboard-rbac-configuration
spec:
  replicas: 1
  selector:
    matchLabels:
      app: dashboard-rbac-configuration
  template:
    metadata:
      labels:
        app: dashboard-rbac-configuration
    spec:
      containers:
      - name: demo
        image: nginx:alpine
        ports:
        - containerPort: 80
