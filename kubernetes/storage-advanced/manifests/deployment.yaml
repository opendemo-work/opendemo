apiVersion: apps/v1
kind: Deployment
metadata:
  name: storage-advanced
  labels:
    app: storage-advanced
spec:
  replicas: 1
  selector:
    matchLabels:
      app: storage-advanced
  template:
    metadata:
      labels:
        app: storage-advanced
    spec:
      containers:
      - name: demo
        image: nginx:alpine
        ports:
        - containerPort: 80
