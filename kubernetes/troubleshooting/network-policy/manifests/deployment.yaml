apiVersion: apps/v1
kind: Deployment
metadata:
  name: network-policy
  labels:
    app: network-policy
spec:
  replicas: 1
  selector:
    matchLabels:
      app: network-policy
  template:
    metadata:
      labels:
        app: network-policy
    spec:
      containers:
      - name: demo
        image: nginx:alpine
        ports:
        - containerPort: 80
