apiVersion: apps/v1
kind: Deployment
metadata:
  name: scheduling-failure
  labels:
    app: scheduling-failure
spec:
  replicas: 1
  selector:
    matchLabels:
      app: scheduling-failure
  template:
    metadata:
      labels:
        app: scheduling-failure
    spec:
      containers:
      - name: demo
        image: nginx:alpine
        ports:
        - containerPort: 80
