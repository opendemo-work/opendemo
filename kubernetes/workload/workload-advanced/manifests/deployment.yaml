apiVersion: apps/v1
kind: Deployment
metadata:
  name: workload-advanced
  labels:
    app: workload-advanced
spec:
  replicas: 1
  selector:
    matchLabels:
      app: workload-advanced
  template:
    metadata:
      labels:
        app: workload-advanced
    spec:
      containers:
      - name: demo
        image: nginx:alpine
        ports:
        - containerPort: 80
