apiVersion: apps/v1
kind: Deployment
metadata:
  name: deployment-basics
  labels:
    app: deployment-basics
spec:
  replicas: 1
  selector:
    matchLabels:
      app: deployment-basics
  template:
    metadata:
      labels:
        app: deployment-basics
    spec:
      containers:
      - name: demo
        image: nginx:alpine
        ports:
        - containerPort: 80
