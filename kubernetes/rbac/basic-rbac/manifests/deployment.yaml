apiVersion: apps/v1
kind: Deployment
metadata:
  name: basic-rbac
  labels:
    app: basic-rbac
spec:
  replicas: 1
  selector:
    matchLabels:
      app: basic-rbac
  template:
    metadata:
      labels:
        app: basic-rbac
    spec:
      containers:
      - name: demo
        image: nginx:alpine
        ports:
        - containerPort: 80
