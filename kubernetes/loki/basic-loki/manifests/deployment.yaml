apiVersion: apps/v1
kind: Deployment
metadata:
  name: basic-loki
  labels:
    app: basic-loki
spec:
  replicas: 1
  selector:
    matchLabels:
      app: basic-loki
  template:
    metadata:
      labels:
        app: basic-loki
    spec:
      containers:
      - name: demo
        image: nginx:alpine
        ports:
        - containerPort: 80
