apiVersion: apps/v1
kind: Deployment
metadata:
  name: coredns-advanced-features
  labels:
    app: coredns-advanced-features
spec:
  replicas: 1
  selector:
    matchLabels:
      app: coredns-advanced-features
  template:
    metadata:
      labels:
        app: coredns-advanced-features
    spec:
      containers:
      - name: demo
        image: nginx:alpine
        ports:
        - containerPort: 80
