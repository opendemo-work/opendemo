apiVersion: apps/v1
kind: Deployment
metadata:
  name: network-troubleshooting
  labels:
    app: network-troubleshooting
spec:
  replicas: 1
  selector:
    matchLabels:
      app: network-troubleshooting
  template:
    metadata:
      labels:
        app: network-troubleshooting
    spec:
      containers:
      - name: demo
        image: nginx:alpine
        ports:
        - containerPort: 80
