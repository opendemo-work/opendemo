apiVersion: apps/v1
kind: Deployment
metadata:
  name: network-production
  labels:
    app: network-production
spec:
  replicas: 1
  selector:
    matchLabels:
      app: network-production
  template:
    metadata:
      labels:
        app: network-production
    spec:
      containers:
      - name: demo
        image: nginx:alpine
        ports:
        - containerPort: 80
