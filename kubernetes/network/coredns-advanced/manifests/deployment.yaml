apiVersion: apps/v1
kind: Deployment
metadata:
  name: coredns-advanced
  labels:
    app: coredns-advanced
spec:
  replicas: 1
  selector:
    matchLabels:
      app: coredns-advanced
  template:
    metadata:
      labels:
        app: coredns-advanced
    spec:
      containers:
      - name: demo
        image: nginx:alpine
        ports:
        - containerPort: 80
