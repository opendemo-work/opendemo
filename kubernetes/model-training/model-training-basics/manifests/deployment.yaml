apiVersion: apps/v1
kind: Deployment
metadata:
  name: model-training-basics
  labels:
    app: model-training-basics
spec:
  replicas: 1
  selector:
    matchLabels:
      app: model-training-basics
  template:
    metadata:
      labels:
        app: model-training-basics
    spec:
      containers:
      - name: demo
        image: nginx:alpine
        ports:
        - containerPort: 80
