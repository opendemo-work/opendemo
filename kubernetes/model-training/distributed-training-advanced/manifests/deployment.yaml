apiVersion: apps/v1
kind: Deployment
metadata:
  name: distributed-training-advanced
  labels:
    app: distributed-training-advanced
spec:
  replicas: 1
  selector:
    matchLabels:
      app: distributed-training-advanced
  template:
    metadata:
      labels:
        app: distributed-training-advanced
    spec:
      containers:
      - name: demo
        image: nginx:alpine
        ports:
        - containerPort: 80
