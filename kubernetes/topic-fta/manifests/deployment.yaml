apiVersion: apps/v1
kind: Deployment
metadata:
  name: topic-fta
  labels:
    app: topic-fta
spec:
  replicas: 1
  selector:
    matchLabels:
      app: topic-fta
  template:
    metadata:
      labels:
        app: topic-fta
    spec:
      containers:
      - name: demo
        image: nginx:alpine
        ports:
        - containerPort: 80
