apiVersion: apps/v1
kind: Deployment
metadata:
  name: prometheus-grafana
  labels:
    app: prometheus-grafana
spec:
  replicas: 1
  selector:
    matchLabels:
      app: prometheus-grafana
  template:
    metadata:
      labels:
        app: prometheus-grafana
    spec:
      containers:
      - name: demo
        image: nginx:alpine
        ports:
        - containerPort: 80
