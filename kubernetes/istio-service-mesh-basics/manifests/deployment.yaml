apiVersion: apps/v1
kind: Deployment
metadata:
  name: istio-service-mesh-basics
  labels:
    app: istio-service-mesh-basics
spec:
  replicas: 1
  selector:
    matchLabels:
      app: istio-service-mesh-basics
  template:
    metadata:
      labels:
        app: istio-service-mesh-basics
    spec:
      containers:
      - name: demo
        image: nginx:alpine
        ports:
        - containerPort: 80
