apiVersion: apps/v1
kind: Deployment
metadata:
  name: basic-crd
  labels:
    app: basic-crd
spec:
  replicas: 1
  selector:
    matchLabels:
      app: basic-crd
  template:
    metadata:
      labels:
        app: basic-crd
    spec:
      containers:
      - name: demo
        image: nginx:alpine
        ports:
        - containerPort: 80
