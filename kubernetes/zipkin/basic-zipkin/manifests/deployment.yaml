apiVersion: apps/v1
kind: Deployment
metadata:
  name: basic-zipkin
  labels:
    app: basic-zipkin
spec:
  replicas: 1
  selector:
    matchLabels:
      app: basic-zipkin
  template:
    metadata:
      labels:
        app: basic-zipkin
    spec:
      containers:
      - name: demo
        image: nginx:alpine
        ports:
        - containerPort: 80
