apiVersion: apps/v1
kind: Deployment
metadata:
  name: ai-infra-advanced
  labels:
    app: ai-infra-advanced
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ai-infra-advanced
  template:
    metadata:
      labels:
        app: ai-infra-advanced
    spec:
      containers:
      - name: demo
        image: nginx:alpine
        ports:
        - containerPort: 80
