apiVersion: apps/v1
kind: Deployment
metadata:
  name: docker-advanced
  labels:
    app: docker-advanced
spec:
  replicas: 1
  selector:
    matchLabels:
      app: docker-advanced
  template:
    metadata:
      labels:
        app: docker-advanced
    spec:
      containers:
      - name: demo
        image: nginx:alpine
        ports:
        - containerPort: 80
