apiVersion: apps/v1
kind: Deployment
metadata:
  name: linux-advanced
  labels:
    app: linux-advanced
spec:
  replicas: 1
  selector:
    matchLabels:
      app: linux-advanced
  template:
    metadata:
      labels:
        app: linux-advanced
    spec:
      containers:
      - name: demo
        image: nginx:alpine
        ports:
        - containerPort: 80
