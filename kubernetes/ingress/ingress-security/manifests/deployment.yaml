apiVersion: apps/v1
kind: Deployment
metadata:
  name: ingress-security
  labels:
    app: ingress-security
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ingress-security
  template:
    metadata:
      labels:
        app: ingress-security
    spec:
      containers:
      - name: demo
        image: nginx:alpine
        ports:
        - containerPort: 80
