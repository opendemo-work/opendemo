apiVersion: apps/v1
kind: Deployment
metadata:
  name: ingress-basics
  labels:
    app: ingress-basics
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ingress-basics
  template:
    metadata:
      labels:
        app: ingress-basics
    spec:
      containers:
      - name: demo
        image: nginx:alpine
        ports:
        - containerPort: 80
