apiVersion: apps/v1
kind: Deployment
metadata:
  name: ingress-troubleshooting
  labels:
    app: ingress-troubleshooting
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ingress-troubleshooting
  template:
    metadata:
      labels:
        app: ingress-troubleshooting
    spec:
      containers:
      - name: demo
        image: nginx:alpine
        ports:
        - containerPort: 80
