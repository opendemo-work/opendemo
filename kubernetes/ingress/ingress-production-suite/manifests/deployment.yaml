apiVersion: apps/v1
kind: Deployment
metadata:
  name: ingress-production-suite
  labels:
    app: ingress-production-suite
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ingress-production-suite
  template:
    metadata:
      labels:
        app: ingress-production-suite
    spec:
      containers:
      - name: demo
        image: nginx:alpine
        ports:
        - containerPort: 80
