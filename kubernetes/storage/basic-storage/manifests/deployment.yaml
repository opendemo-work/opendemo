apiVersion: apps/v1
kind: Deployment
metadata:
  name: basic-storage
  labels:
    app: basic-storage
spec:
  replicas: 1
  selector:
    matchLabels:
      app: basic-storage
  template:
    metadata:
      labels:
        app: basic-storage
    spec:
      containers:
      - name: demo
        image: nginx:alpine
        ports:
        - containerPort: 80
