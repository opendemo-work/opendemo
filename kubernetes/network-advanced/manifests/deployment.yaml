apiVersion: apps/v1
kind: Deployment
metadata:
  name: network-advanced
  labels:
    app: network-advanced
spec:
  replicas: 1
  selector:
    matchLabels:
      app: network-advanced
  template:
    metadata:
      labels:
        app: network-advanced
    spec:
      containers:
      - name: demo
        image: nginx:alpine
        ports:
        - containerPort: 80
