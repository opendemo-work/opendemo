apiVersion: apps/v1
kind: Deployment
metadata:
  name: basic-llmops
  labels:
    app: basic-llmops
spec:
  replicas: 1
  selector:
    matchLabels:
      app: basic-llmops
  template:
    metadata:
      labels:
        app: basic-llmops
    spec:
      containers:
      - name: demo
        image: nginx:alpine
        ports:
        - containerPort: 80
