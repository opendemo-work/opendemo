apiVersion: apps/v1
kind: Deployment
metadata:
  name: topic-learn
  labels:
    app: topic-learn
spec:
  replicas: 1
  selector:
    matchLabels:
      app: topic-learn
  template:
    metadata:
      labels:
        app: topic-learn
    spec:
      containers:
      - name: demo
        image: nginx:alpine
        ports:
        - containerPort: 80
