apiVersion: apps/v1
kind: Deployment
metadata:
  name: service-production-suite
  labels:
    app: service-production-suite
spec:
  replicas: 1
  selector:
    matchLabels:
      app: service-production-suite
  template:
    metadata:
      labels:
        app: service-production-suite
    spec:
      containers:
      - name: demo
        image: nginx:alpine
        ports:
        - containerPort: 80
