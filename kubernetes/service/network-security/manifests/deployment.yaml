apiVersion: apps/v1
kind: Deployment
metadata:
  name: network-security
  labels:
    app: network-security
spec:
  replicas: 1
  selector:
    matchLabels:
      app: network-security
  template:
    metadata:
      labels:
        app: network-security
    spec:
      containers:
      - name: demo
        image: nginx:alpine
        ports:
        - containerPort: 80
