apiVersion: apps/v1
kind: Deployment
metadata:
  name: backup-hooks
  labels:
    app: backup-hooks
spec:
  replicas: 1
  selector:
    matchLabels:
      app: backup-hooks
  template:
    metadata:
      labels:
        app: backup-hooks
    spec:
      containers:
      - name: demo
        image: nginx:alpine
        ports:
        - containerPort: 80
