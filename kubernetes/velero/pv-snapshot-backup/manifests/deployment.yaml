apiVersion: apps/v1
kind: Deployment
metadata:
  name: pv-snapshot-backup
  labels:
    app: pv-snapshot-backup
spec:
  replicas: 1
  selector:
    matchLabels:
      app: pv-snapshot-backup
  template:
    metadata:
      labels:
        app: pv-snapshot-backup
    spec:
      containers:
      - name: demo
        image: nginx:alpine
        ports:
        - containerPort: 80
