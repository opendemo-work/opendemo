apiVersion: apps/v1
kind: Deployment
metadata:
  name: resource-filtering
  labels:
    app: resource-filtering
spec:
  replicas: 1
  selector:
    matchLabels:
      app: resource-filtering
  template:
    metadata:
      labels:
        app: resource-filtering
    spec:
      containers:
      - name: demo
        image: nginx:alpine
        ports:
        - containerPort: 80
