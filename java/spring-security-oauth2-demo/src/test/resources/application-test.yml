spring:
  datasource:
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password:
  h2:
    console:
      enabled: false
  sql:
    init:
      mode: always

server:
  port: 0

jwt:
  secret: testSecretKeyForJwtTokenGenerationAndValidationMustBe256Bits!!

logging:
  level:
    com.example.demo: WARN
