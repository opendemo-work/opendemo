server:
  port: 8081

order:
  max-items: 500
  timeout-seconds: 60
  default-currency: CNY

database:
  host: localhost
  port: 3306
  name: order_db_dev

logging:
  level:
    root: DEBUG
    com.example: TRACE
